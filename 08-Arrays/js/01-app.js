const numeros = [10,20,30]

// Sintaxis no tan comun pero la podemos encontrar

const meses = new Array('Enero','Febrero','Marzo')

//  Los arreglos pueden tener datos de todo tipo

const deTodo = ['hola', true, 10, null, {nombre: 'pedro', edad:20}]