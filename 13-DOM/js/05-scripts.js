const card = document.querySelectorAll('.card')
console.log(card)

const form = document.querySelectorAll('#formulario')
console.log(form)

// No existe con querySelectorAll
const noExiste = document.querySelectorAll('.no-existe')
console.log(noExiste)