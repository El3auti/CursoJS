const producto = {
    nombre: "Monitor 20'",
    precio: 400,
    disponible: true,
}

console.log(Object.keys(producto)) // Nos retorna un arreglo  con las llaves del obj

console.log(Object.values(producto)) // Nos retorna un arreglo con los valores del obj

console.log(Object.entries(producto)) // 