let tareas = ['Comer', 'Estudiar','Hacer Deporte', 'Ir a la escuela']

/**
 * For of no es como el for normal, este ejecuta mientras tengas elementos por iterar
 * como un arreglo, maps o sets
 */
 
for(let tarea of tareas){
     console.log(tarea)
    }